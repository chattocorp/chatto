---
title: BotService
description: Bot account lifecycle and API-key management RPCs.
editUrl: false
---

{/* Generated from proto/chatto/{auth,discovery,api,admin,realtime}/v1/*.proto. Do not edit directly. */}

Chatto exposes this service below `/api/connect`.



Shared message and enum definitions are documented in [Shared Types And Enums](/reference/connectrpc-api/types/).



<a id="chatto-api-v1-BotService"></a>

## BotService

Creates and manages bot accounts owned by human users. Bot API keys cannot
call this service.

<a id="chatto-api-v1-BotService-ListBotWebhookFailures"></a>

### ListBotWebhookFailures

List retained failures for an endpoint of a bot you can manage. Returns full
records in recording order, oldest first. Expired records are omitted.
This history is diagnostic; an empty result does not prove successful delivery.

```http
POST /api/connect/chatto.api.v1.BotService/ListBotWebhookFailures
```

<a id="chatto-api-v1-ListBotWebhookFailuresRequest"></a>

#### Input: ListBotWebhookFailuresRequest

Read the retained failure history for a current endpoint.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | No field description provided. |
| `webhook_id` | `string` | No field description provided. |
| `page_size` | `uint32` | Maximum records, from 1 to 100. Zero selects 20. |
| `cursor` | `string` | Opaque continuation from the previous response. Bound to viewer and endpoint. |


<a id="chatto-api-v1-ListBotWebhookFailuresResponse"></a>

#### Result: ListBotWebhookFailuresResponse

A bounded page of complete failure records. No per-record hydration is needed.

| Field | Type | Description |
| --- | --- | --- |
| `failures` | repeated [`BotWebhookFailure`](/reference/connectrpc-api/types/#chatto-api-v1-BotWebhookFailure) | No field description provided. |
| `next_cursor` | `string` | Empty at the end. Refresh without a cursor to include newer records. |


<a id="chatto-api-v1-BotService-ListBotOutboundWebhooks"></a>

### ListBotOutboundWebhooks

Lists all endpoints, including paused endpoints. Requires bot ownership or bot.manage.
Returns the complete bounded collection, so callers do not need batch hydration.

```http
POST /api/connect/chatto.api.v1.BotService/ListBotOutboundWebhooks
```

<a id="chatto-api-v1-ListBotOutboundWebhooksRequest"></a>

#### Input: ListBotOutboundWebhooksRequest

Read all endpoints for one managed bot. At most 20 endpoints are returned.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required managed bot ID. |


<a id="chatto-api-v1-ListBotOutboundWebhooksResponse"></a>

#### Result: ListBotOutboundWebhooksResponse

All current endpoints, including paused ones.

| Field | Type | Description |
| --- | --- | --- |
| `webhooks` | repeated [`BotOutboundWebhook`](/reference/connectrpc-api/types/#chatto-api-v1-BotOutboundWebhook) | Complete collection, ordered by creation time and ID. No pagination is needed. |


<a id="chatto-api-v1-BotService-GetBotOutboundWebhook"></a>

### GetBotOutboundWebhook

Gets one endpoint. Requires bot ownership or bot.manage. Missing endpoints return NOT_FOUND.

```http
POST /api/connect/chatto.api.v1.BotService/GetBotOutboundWebhook
```

<a id="chatto-api-v1-GetBotOutboundWebhookRequest"></a>

#### Input: GetBotOutboundWebhookRequest

Read one endpoint belonging to the given managed bot.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required managed bot ID. |
| `webhook_id` | `string` | Required endpoint ID within this bot. |


<a id="chatto-api-v1-GetBotOutboundWebhookResponse"></a>

#### Result: GetBotOutboundWebhookResponse

Metadata for the requested endpoint.

| Field | Type | Description |
| --- | --- | --- |
| `webhook` | [`BotOutboundWebhook`](/reference/connectrpc-api/types/#chatto-api-v1-BotOutboundWebhook) | Endpoint metadata without Authorization or signing credentials. |


<a id="chatto-api-v1-BotService-CreateBotOutboundWebhook"></a>

### CreateBotOutboundWebhook

Creates an endpoint and returns its signing secret once. Requires bot ownership or bot.manage.

```http
POST /api/connect/chatto.api.v1.BotService/CreateBotOutboundWebhook
```

<a id="chatto-api-v1-CreateBotOutboundWebhookRequest"></a>

#### Input: CreateBotOutboundWebhookRequest

Create an independent endpoint with its own signing secret.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required managed bot ID. |
| `url` | `string` | Absolute HTTPS destination; HTTP is also allowed for localhost names. |
| `authorization` | `string` | Optional complete Authorization header value. |
| `enabled` | `bool` | False creates a paused endpoint. |
| `name` | `string` | Display name, fixed after creation. |


<a id="chatto-api-v1-CreateBotOutboundWebhookResponse"></a>

#### Result: CreateBotOutboundWebhookResponse

New endpoint and its show-once request verification secret.

| Field | Type | Description |
| --- | --- | --- |
| `webhook` | [`BotOutboundWebhook`](/reference/connectrpc-api/types/#chatto-api-v1-BotOutboundWebhook) | Endpoint metadata without Authorization or signing credentials. |
| `signing_secret` | `string` | Returned only at creation. Configure the receiver with this HMAC secret if it verifies requests. |


<a id="chatto-api-v1-BotService-UpdateBotOutboundWebhook"></a>

### UpdateBotOutboundWebhook

Edits delivery settings or pauses/resumes delivery. Requires ownership or bot.manage.

```http
POST /api/connect/chatto.api.v1.BotService/UpdateBotOutboundWebhook
```

<a id="chatto-api-v1-UpdateBotOutboundWebhookRequest"></a>

#### Input: UpdateBotOutboundWebhookRequest

Patch delivery settings or pause/resume one endpoint. update_mask selects
fields to apply or reset. Name and signing secret stay fixed.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required managed bot ID. |
| `webhook_id` | `string` | Required endpoint ID within this bot. |
| `enabled` | `optional bool` | Selected enabled state. False pauses delivery; resume accepts only new messages. |
| `url` | `optional string` | Selected new destination. Changing settings cancels queued retries. |
| `authorization` | `optional string` | Selected Authorization header. Absent or empty removes it. Never returned. |
| `update_mask` | `google.protobuf.FieldMask` | Editable fields to apply or reset: enabled, url, authorization. Omit to infer populated fields; * selects all editable fields. An explicit empty mask is invalid. Unselected values are ignored. Selected absent values reset the field to its default, subject to field validation. |


<a id="chatto-api-v1-UpdateBotOutboundWebhookResponse"></a>

#### Result: UpdateBotOutboundWebhookResponse

Endpoint state after the update.

| Field | Type | Description |
| --- | --- | --- |
| `webhook` | [`BotOutboundWebhook`](/reference/connectrpc-api/types/#chatto-api-v1-BotOutboundWebhook) | Endpoint metadata without Authorization or signing credentials. |


<a id="chatto-api-v1-BotService-RevokeBotOutboundWebhook"></a>

### RevokeBotOutboundWebhook

Permanently revokes an endpoint. Requires ownership or bot.manage.

```http
POST /api/connect/chatto.api.v1.BotService/RevokeBotOutboundWebhook
```

<a id="chatto-api-v1-RevokeBotOutboundWebhookRequest"></a>

#### Input: RevokeBotOutboundWebhookRequest

Revoke one endpoint and cancel its queued retries. In-flight HTTP may finish.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required managed bot ID. |
| `webhook_id` | `string` | Required endpoint ID within this bot. |


<a id="chatto-api-v1-RevokeBotOutboundWebhookResponse"></a>

#### Result: RevokeBotOutboundWebhookResponse

Revocation completed, or this endpoint was already absent.


<a id="chatto-api-v1-BotService-ListBots"></a>

### ListBots

Lists bots visible to the authenticated caller.

```http
POST /api/connect/chatto.api.v1.BotService/ListBots
```

<a id="chatto-api-v1-ListBotsRequest"></a>

#### Input: ListBotsRequest

Request bots visible to the caller. Owners see their own bots. Callers with
bot.manage or user.manage-accounts see every bot.

| Field | Type | Description |
| --- | --- | --- |
| `search` | `string` | Optional case-insensitive login or display-name search. |
| `page` | [`PageRequest`](/reference/connectrpc-api/types/#chatto-api-v1-PageRequest) | Page request. Defaults to 20 results when absent or limit is zero. |


<a id="chatto-api-v1-ListBotsResponse"></a>

#### Result: ListBotsResponse

Page of visible bots.

| Field | Type | Description |
| --- | --- | --- |
| `bots` | repeated [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Visible bots. |
| `page` | [`PageInfo`](/reference/connectrpc-api/types/#chatto-api-v1-PageInfo) | Page metadata. |


<a id="chatto-api-v1-BotService-GetBot"></a>

### GetBot

Gets one visible bot. Returns NOT_FOUND for an unknown bot. Returns
PERMISSION_DENIED when the caller is not the owner and has neither
bot.manage nor user.manage-accounts.

```http
POST /api/connect/chatto.api.v1.BotService/GetBot
```

<a id="chatto-api-v1-GetBotRequest"></a>

#### Input: GetBotRequest

Request one bot by stable user ID.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required bot user ID. |


<a id="chatto-api-v1-GetBotResponse"></a>

#### Result: GetBotResponse

Response containing one bot.

| Field | Type | Description |
| --- | --- | --- |
| `bot` | [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Requested bot. |


<a id="chatto-api-v1-BotService-BatchGetBots"></a>

### BatchGetBots

Gets several visible bots. Missing and inaccessible bots are omitted.

```http
POST /api/connect/chatto.api.v1.BotService/BatchGetBots
```

<a id="chatto-api-v1-BatchGetBotsRequest"></a>

#### Input: BatchGetBotsRequest

Request several bots by stable user ID.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_ids` | `repeated string` | Required bot user IDs. Missing or inaccessible bots are omitted. |


<a id="chatto-api-v1-BatchGetBotsResponse"></a>

#### Result: BatchGetBotsResponse

Response containing accessible bots in first-seen request order.

| Field | Type | Description |
| --- | --- | --- |
| `bots` | repeated [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Found bots. |


<a id="chatto-api-v1-BotService-CreateBot"></a>

### CreateBot

Creates a bot owned by the authenticated human caller. Requires bot.create.

```http
POST /api/connect/chatto.api.v1.BotService/CreateBot
```

<a id="chatto-api-v1-CreateBotRequest"></a>

#### Input: CreateBotRequest

Request creation of a bot owned by the authenticated human caller.

| Field | Type | Description |
| --- | --- | --- |
| `login` | `string` | Bot login. Must end in `_bot`. |
| `display_name` | `string` | Public display name. |
| `api_key_name` | `optional string` | Optional name for the initial API key. Defaults to "Default key". |


<a id="chatto-api-v1-CreateBotResponse"></a>

#### Result: CreateBotResponse

Result of creating a bot. api_key is shown once and cannot be retrieved.

| Field | Type | Description |
| --- | --- | --- |
| `bot` | [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Created bot. |
| `api_key` | `string` | Newly issued API key. |
| `api_key_metadata` | [`BotApiKey`](/reference/connectrpc-api/types/#chatto-api-v1-BotApiKey) | Safe metadata for the newly issued API key. |


<a id="chatto-api-v1-BotService-DeleteBot"></a>

### DeleteBot

Deletes a bot and invalidates all of its API keys.

```http
POST /api/connect/chatto.api.v1.BotService/DeleteBot
```

<a id="chatto-api-v1-DeleteBotRequest"></a>

#### Input: DeleteBotRequest

Request deletion of a bot account.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required bot user ID. |


<a id="chatto-api-v1-DeleteBotResponse"></a>

#### Result: DeleteBotResponse

Result of deleting a bot account.

| Field | Type | Description |
| --- | --- | --- |
| `deleted` | `bool` | True when the bot was deleted. |


<a id="chatto-api-v1-BotService-CreateBotApiKey"></a>

### CreateBotApiKey

Creates one named API key. A bot can have at most 20 active API keys.

```http
POST /api/connect/chatto.api.v1.BotService/CreateBotApiKey
```

<a id="chatto-api-v1-CreateBotApiKeyRequest"></a>

#### Input: CreateBotApiKeyRequest

Request creation of one named API key for a bot.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required bot user ID. |
| `name` | `string` | Required manager-defined name. Names do not need to be unique. |


<a id="chatto-api-v1-CreateBotApiKeyResponse"></a>

#### Result: CreateBotApiKeyResponse

Result of creating one API key. api_key is shown once and cannot be
retrieved later.

| Field | Type | Description |
| --- | --- | --- |
| `bot` | [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Updated bot metadata. |
| `api_key` | `string` | Newly issued API key. |
| `api_key_metadata` | [`BotApiKey`](/reference/connectrpc-api/types/#chatto-api-v1-BotApiKey) | Safe metadata for the newly issued API key. |


<a id="chatto-api-v1-BotService-RevokeBotApiKey"></a>

### RevokeBotApiKey

Revokes one API key without changing other active keys.

```http
POST /api/connect/chatto.api.v1.BotService/RevokeBotApiKey
```

<a id="chatto-api-v1-RevokeBotApiKeyRequest"></a>

#### Input: RevokeBotApiKeyRequest

Request irreversible revocation of one bot API key.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required bot user ID. |
| `key_id` | `string` | Required API-key credential ID. |


<a id="chatto-api-v1-RevokeBotApiKeyResponse"></a>

#### Result: RevokeBotApiKeyResponse

Result of revoking one bot API key.

| Field | Type | Description |
| --- | --- | --- |
| `bot` | [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Updated bot metadata. |


<a id="chatto-api-v1-BotService-CreateBotIncomingWebhook"></a>

### CreateBotIncomingWebhook

Creates a named incoming webhook. A bot can have at most 20 active
incoming webhooks.

```http
POST /api/connect/chatto.api.v1.BotService/CreateBotIncomingWebhook
```

<a id="chatto-api-v1-CreateBotIncomingWebhookRequest"></a>

#### Input: CreateBotIncomingWebhookRequest

Request creation of one named incoming webhook for a bot.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required bot user ID. |
| `name` | `string` | Required manager-defined name. Names do not need to be unique. |


<a id="chatto-api-v1-CreateBotIncomingWebhookResponse"></a>

#### Result: CreateBotIncomingWebhookResponse

Result of creating an incoming webhook. webhook_url is shown once and cannot
be retrieved later.

| Field | Type | Description |
| --- | --- | --- |
| `bot` | [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Updated bot metadata. |
| `webhook_url` | `string` | Newly issued incoming webhook URL. |


<a id="chatto-api-v1-BotService-RevokeBotIncomingWebhook"></a>

### RevokeBotIncomingWebhook

Revokes one incoming webhook without changing other webhooks.

```http
POST /api/connect/chatto.api.v1.BotService/RevokeBotIncomingWebhook
```

<a id="chatto-api-v1-RevokeBotIncomingWebhookRequest"></a>

#### Input: RevokeBotIncomingWebhookRequest

Request irreversible revocation of one incoming webhook credential.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required bot user ID. |
| `webhook_id` | `string` | Required webhook credential ID. |


<a id="chatto-api-v1-RevokeBotIncomingWebhookResponse"></a>

#### Result: RevokeBotIncomingWebhookResponse

Result of revoking an incoming webhook.

| Field | Type | Description |
| --- | --- | --- |
| `bot` | [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Updated bot metadata. |


<a id="chatto-api-v1-BotService-ReassignBotOwner"></a>

### ReassignBotOwner

Reassigns a bot to another active human owner. Requires bot.manage. The
current API keys and configured permission allowlist remain unchanged,
while effective permissions immediately use the new owner's ceiling.

```http
POST /api/connect/chatto.api.v1.BotService/ReassignBotOwner
```

<a id="chatto-api-v1-ReassignBotOwnerRequest"></a>

#### Input: ReassignBotOwnerRequest

Request an administrative reassignment of a bot to another human owner.

| Field | Type | Description |
| --- | --- | --- |
| `bot_user_id` | `string` | Required bot user ID. |
| `owner_user_id` | `string` | Required user ID of the new human owner. |


<a id="chatto-api-v1-ReassignBotOwnerResponse"></a>

#### Result: ReassignBotOwnerResponse

Result of reassigning a bot owner. The bot's API keys and configured
permission allowlist are unchanged.

| Field | Type | Description |
| --- | --- | --- |
| `bot` | [`Bot`](/reference/connectrpc-api/types/#chatto-api-v1-Bot) | Reassigned bot. |
