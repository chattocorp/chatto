{
  "worktree": {
    "setup": "mise trust && mise install && mise setup && (cd authling && mise trust && mise install && mise deps)",
    "teardown": "mise dev-archive"
  },
  "scripts": {
    "dev": {
      "command": "exec mise dev"
    }
  },
  "metadataGeneration": {
    "branchName": {
      "instructions": "Use conventional commit branch prefixes, eg. fix/, feat/, perf/ etc."
    }
  }
}
