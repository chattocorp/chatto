---
title: PermissionService
description: Read-only effective permissions for bot and human accounts.
editUrl: false
---

{/* Generated from proto/chatto/{auth,discovery,api,admin,realtime}/v1/*.proto. Do not edit directly. */}

Chatto exposes this service below `/api/connect`.



Shared message and enum definitions are documented in [Shared Types And Enums](/reference/connectrpc-api/types/).



<a id="chatto-api-v1-PermissionService"></a>

## PermissionService

Read-only effective authority. Configuration and explanations remain in
chatto.admin.v1.AdminPermissionService.

<a id="chatto-api-v1-PermissionService-ListEffectivePermissions"></a>

### ListEffectivePermissions

Any authenticated member may read a bot's effective permissions. Human
targets require user.manage-permissions, including self-targeted reads.
Bot authority includes the owner's current limits. Hidden room identities,
stored grants, credentials, and individual DM participants are not exposed.
Missing or deleted targets return NOT_FOUND. Each page is a fresh read;
concurrent changes can move entries between pages. Clients should refresh
visible views (the bundled client uses 30 seconds), deduplicate overlapping
pages, and clear the view on close or read failure.

```http
POST /api/connect/chatto.api.v1.PermissionService/ListEffectivePermissions
```

<a id="chatto-api-v1-ListEffectivePermissionsRequest"></a>

#### Input: ListEffectivePermissionsRequest

Select a current page of effective permissions for one account.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Required target account ID. |
| `page` | [`PageRequest`](/reference/connectrpc-api/types/#chatto-api-v1-PageRequest) | Offset page over visible permission entries; default 20, maximum 100. |


<a id="chatto-api-v1-ListEffectivePermissionsResponse"></a>

#### Result: ListEffectivePermissionsResponse

Effective grants in permission-catalog order, then stable scope-ID order.
Denied and unconfigured permissions are absent. Inherited and included
permissions remain separate entries; clients can group them for display.

| Field | Type | Description |
| --- | --- | --- |
| `permissions` | repeated [`EffectivePermission`](/reference/connectrpc-api/types/#chatto-api-v1-EffectivePermission) | Effective grants visible to the caller. |
| `page` | [`PageInfo`](/reference/connectrpc-api/types/#chatto-api-v1-PageInfo) | Counts and continuation for the visible entry collection. |
