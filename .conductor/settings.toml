"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[[preview_urls]]
name = "Chatto"
url = "https://chatto.$CONDUCTOR_WORKSPACE_NAME.localhost:42444"

[[preview_urls]]
name = "Authling"
url = "https://authling.$CONDUCTOR_WORKSPACE_NAME.localhost:42444"

[[preview_urls]]
name = "Mailpit"
url = "https://mailpit.$CONDUCTOR_WORKSPACE_NAME.localhost:42444"

[[preview_urls]]
name = "LiveKit"
url = "https://livekit.$CONDUCTOR_WORKSPACE_NAME.localhost:42444"

[[preview_urls]]
name = "Storybook"
url = "https://storybook.$CONDUCTOR_WORKSPACE_NAME.localhost:42444"

[[preview_urls]]
name = "Docs website"
url = "https://docs.$CONDUCTOR_WORKSPACE_NAME.localhost:42444"

[scripts]
setup = "mise trust && mise install && mise setup && (cd authling && mise trust && mise install && mise deps)"
archive = "exec tools/stop-workspace-dev.sh"
run_mode = "concurrent"

[scripts.run.dev]
command = "exec mise dev"
available_in = [ "local" ]
default = true
icon = "network"

[scripts.run.storybook]
command = "exec mise storybook"
available_in = [ "local" ]
icon = "component"

[scripts.run.docs_website]
command = "exec mise dev-docs-website"
available_in = [ "local" ]
icon = "book-open"

[prompts]
create_pr = "Create a full GitHub pull request for the current branch, not a draft PR."

[git]
delete_branch_on_archive = false
worktree_push_auto_setup_remote = true
