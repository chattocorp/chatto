// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chatto/api/v1/permissions.proto

package apiv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "hmans.de/chatto/internal/pb/chatto/api/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PermissionServiceName is the fully-qualified name of the PermissionService service.
	PermissionServiceName = "chatto.api.v1.PermissionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PermissionServiceListEffectivePermissionsProcedure is the fully-qualified name of the
	// PermissionService's ListEffectivePermissions RPC.
	PermissionServiceListEffectivePermissionsProcedure = "/chatto.api.v1.PermissionService/ListEffectivePermissions"
)

// PermissionServiceClient is a client for the chatto.api.v1.PermissionService service.
type PermissionServiceClient interface {
	// Any authenticated member may read a bot's effective permissions. Human
	// targets require user.manage-permissions, including self-targeted reads.
	// Bot authority includes the owner's current limits. Hidden room identities,
	// stored grants, credentials, and individual DM participants are not exposed.
	// Missing or deleted targets return NOT_FOUND. Each call returns the complete
	// visible set from one coherent read, without pagination or truncation. Clients
	// should refresh visible views (the bundled client uses 30 seconds) and clear
	// the view on close or read failure.
	ListEffectivePermissions(context.Context, *connect.Request[v1.ListEffectivePermissionsRequest]) (*connect.Response[v1.ListEffectivePermissionsResponse], error)
}

// NewPermissionServiceClient constructs a client for the chatto.api.v1.PermissionService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPermissionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PermissionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	permissionServiceMethods := v1.File_chatto_api_v1_permissions_proto.Services().ByName("PermissionService").Methods()
	return &permissionServiceClient{
		listEffectivePermissions: connect.NewClient[v1.ListEffectivePermissionsRequest, v1.ListEffectivePermissionsResponse](
			httpClient,
			baseURL+PermissionServiceListEffectivePermissionsProcedure,
			connect.WithSchema(permissionServiceMethods.ByName("ListEffectivePermissions")),
			connect.WithClientOptions(opts...),
		),
	}
}

// permissionServiceClient implements PermissionServiceClient.
type permissionServiceClient struct {
	listEffectivePermissions *connect.Client[v1.ListEffectivePermissionsRequest, v1.ListEffectivePermissionsResponse]
}

// ListEffectivePermissions calls chatto.api.v1.PermissionService.ListEffectivePermissions.
func (c *permissionServiceClient) ListEffectivePermissions(ctx context.Context, req *connect.Request[v1.ListEffectivePermissionsRequest]) (*connect.Response[v1.ListEffectivePermissionsResponse], error) {
	return c.listEffectivePermissions.CallUnary(ctx, req)
}

// PermissionServiceHandler is an implementation of the chatto.api.v1.PermissionService service.
type PermissionServiceHandler interface {
	// Any authenticated member may read a bot's effective permissions. Human
	// targets require user.manage-permissions, including self-targeted reads.
	// Bot authority includes the owner's current limits. Hidden room identities,
	// stored grants, credentials, and individual DM participants are not exposed.
	// Missing or deleted targets return NOT_FOUND. Each call returns the complete
	// visible set from one coherent read, without pagination or truncation. Clients
	// should refresh visible views (the bundled client uses 30 seconds) and clear
	// the view on close or read failure.
	ListEffectivePermissions(context.Context, *connect.Request[v1.ListEffectivePermissionsRequest]) (*connect.Response[v1.ListEffectivePermissionsResponse], error)
}

// NewPermissionServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPermissionServiceHandler(svc PermissionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	permissionServiceMethods := v1.File_chatto_api_v1_permissions_proto.Services().ByName("PermissionService").Methods()
	permissionServiceListEffectivePermissionsHandler := connect.NewUnaryHandler(
		PermissionServiceListEffectivePermissionsProcedure,
		svc.ListEffectivePermissions,
		connect.WithSchema(permissionServiceMethods.ByName("ListEffectivePermissions")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chatto.api.v1.PermissionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PermissionServiceListEffectivePermissionsProcedure:
			permissionServiceListEffectivePermissionsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPermissionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPermissionServiceHandler struct{}

func (UnimplementedPermissionServiceHandler) ListEffectivePermissions(context.Context, *connect.Request[v1.ListEffectivePermissionsRequest]) (*connect.Response[v1.ListEffectivePermissionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("chatto.api.v1.PermissionService.ListEffectivePermissions is not implemented"))
}
