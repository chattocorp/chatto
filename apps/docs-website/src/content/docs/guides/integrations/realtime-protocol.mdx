---
title: Realtime Protocol Overview
description: Receive canonical events, choose initial state, resume a short gap, and hydrate room state.
---

import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";
import realtimeSequence from "../../../../assets/realtime-protocol-sequence.svg?raw";

Use realtime protocol 4 when a bot or client must react to activity on one
Chatto server. The protocol sends binary protobuf frames over a WebSocket at
`/api/realtime`.

Use [ConnectRPC](/reference/connectrpc-api/) for commands, explicit resource
reads, pagination, history, and read-your-writes results. Realtime events tell
you what happened. ConnectRPC gives you the resource detail that your
integration needs.

| Need                                                                     | Use               |
| ------------------------------------------------------------------------ | ----------------- |
| React to a message, edit, reaction, membership change, or other activity | Realtime event    |
| Create, update, or delete a resource                                     | ConnectRPC        |
| Read one resource or a paginated list                                    | ConnectRPC        |
| Build the bundled frontend's initial local view                          | Realtime snapshot |
| Repair a short connection gap                                            | Realtime resume   |

## Connection lifecycle

<div style="margin-block:1.5rem;overflow-x:auto" set:html={realtimeSequence} />

1. Open a WebSocket to `wss://chat.example.com/api/realtime`.
2. Send a binary [`RealtimeClientFrame`](/reference/connectrpc-api/realtime/#realtimeclientframe)
   with `hello`. Request protocol version `4`. Include a bearer credential when
   you do not use a same-origin cookie.
3. Wait for the server `hello`. Check that it accepts version `4`.
4. Send `subscribe_events`. Select `SNAPSHOT` or `LIVE_ONLY` as the required
   initial-state fallback. You can also include a safe resume cursor and the
   IDs of room timelines that your projection retains.
5. Read `subscribed.recovery_mode` to learn whether the server selected
   `SNAPSHOT`, `LIVE_ONLY`, or `RESUME`.
6. Process events and state in order. The server sends `caught_up` when recovery
   reaches the live boundary.
7. Continue to process live events.

The `subscribed` frame does not mean that recovery is complete. Treat the
stream as live only after `caught_up`.

## Choose snapshot or live-only startup

Select `LIVE_ONLY` for a bot that wants new events and reads resources through
ConnectRPC. If the cursor is absent or unusable, the server starts at a stated
current boundary. It does not send old events or a state snapshot.

Select `SNAPSHOT` for a client that keeps a server projection. If the cursor is
absent or unusable, the server sends authorized current-state items before
`caught_up`. The bundled frontend uses this mode.

When a cursor is usable, both choices can result in `RESUME`. The server sends
authorized durable events after that cursor, sends finite latest-value state,
and then sends `caught_up`.

A cursor can become unusable because it is invalid, expired, too far behind,
from another server history, or unsafe for current authorization. Fallback is
normal recovery behavior. The server does not send a partial replay.

<Aside type="caution">
  Keep a resume cursor with the client state that it describes. Do not apply a
  cursor to an unrelated projection. A cursor is valid for up to 24 hours, and
  the server binds it to the authenticated viewer.
</Aside>

## Handle canonical events

Each `RealtimeEvent` carries one authorized copy of Chatto's canonical Event.
Durable variants
include messages, edits, retractions, reactions, pins, assets, rooms,
membership, profiles, threads, and calls. Typing, presence changes, and session
termination are transient variants and have no resume cursor.

The canonical Event contains common metadata outside its event `oneof`:

- a stable event ID;
- source time;
- a visible actor ID, when applicable.

The `RealtimeEvent` transport wrapper contains the resume cursor for durable
activity and optional current state. The canonical Event does not contain
broker coordinates or a cursor.

The server omits internal event variants and storage-only fields. It can add
authorized delivery-only `_plaintext` fields. It never sends raw EVT bytes,
ciphertext, credential data, subjects, or JetStream sequences.

An event can also contain current `state` items. A bot can ignore this state
and fetch resources through ConnectRPC. A projection client can apply it to
converge without extra reads.

Process one event as one unit:

1. Decode the complete frame.
2. Process the canonical variant that you understand.
3. Apply any current-state items that your client uses.
4. Save `resume_cursor` only after your processing succeeds.

Delivery is at least once across reconnects. Use the stable event ID to
deduplicate effects.

New event and state variants are additive. You can ignore a variant that you
do not use and still save the common cursor. If a complete top-level frame
cannot be decoded, close the socket and keep the preceding cursor.

## Apply snapshot state

If `subscribed.recovery_mode` is `SNAPSHOT`, clear the state that the realtime
projection owns. Then apply each standalone `state` frame until `caught_up`.
These frames contain current resources, not historical events.

Snapshot state can include the server, viewer, users, rooms, room groups,
notifications, calls, presence, and retained room timelines. A state item can
replace a finite set, upsert a resource, or report that a resource is absent.

Do not start parallel ConnectRPC refreshes for the same canonical state while
you apply a snapshot. Use ConnectRPC for data that is not part of the snapshot,
such as old history, searches, and command responses.

## Retain room state only when needed

The initial snapshot contains lightweight state for visible rooms. It does not
contain every room member list or timeline. Send `hydrate_room` after
`caught_up` when a client first needs a joined room's complete current member
IDs and recent timeline.

The server returns standalone `room` and `room_timeline` state frames. After
you apply both, keep that room ID in your retained set and send the set in
later subscriptions. One connection can retain at most 64 room timelines.

Retained room IDs affect only current timeline state. They never filter
canonical events. A bot receives an authorized message or reaction event even
when it does not retain that room's timeline.

Hydration admission errors are non-fatal. They identify the room and can
include `retry_after_ms`. Retry that room after the delay. A
`room_unavailable` result means that the room does not exist or the viewer
cannot access it.

## Detect a dead connection

The server `hello` reports an approximate heartbeat interval. Reset your
liveness timer after every server frame. Replace the socket when no frame
arrives for several expected heartbeat intervals.

You can send `ping` with an opaque nonce. The server returns the same nonce in
`pong`. WebSocket control pings do not replace the application heartbeat.

## Handle errors and reconnects

| Frame                           | Client action                                                                                                                                         |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Non-fatal `error`               | Keep the socket open. Retry only the rejected operation and honor `retry_after_ms`.                                                                   |
| Fatal `error`                   | Stop processing that socket and keep the preceding cursor. Retry temporary failures with backoff. Fix protocol or authentication errors before retry. |
| `close` with `reconnect: true`  | Close the socket, wait for `retry_after_ms` or local backoff, and subscribe with the last safe cursor.                                                |
| `close` with `reconnect: false` | Stop automatic reconnects and require the applicable user or authentication action.                                                                   |
| Network close without guidance  | Retry with bounded exponential backoff and the last safe cursor.                                                                                      |

Bearer access credentials can expire while the socket is open. Rotate the
credential and reconnect when the server requests authentication. Same-origin
cookie clients can receive a session-renewal close and must renew the browser
session before they reconnect.

<CardGrid>
  <LinkCard
    title="TypeScript Tutorial"
    href="/guides/integrations/realtime-typescript/"
    description="Connect a small event-driven bot client and resume from a cursor."
  />
  <LinkCard
    title="Realtime Frame Reference"
    href="/reference/connectrpc-api/realtime/"
    description="Read every realtime protobuf message, field, and enum."
  />
  <LinkCard
    title="API Compatibility"
    href="/guides/integrations/api-compatibility/"
    description="Plan for API and protocol changes while Chatto is pre-1.0."
  />
</CardGrid>
