diff --git a/dist/index.cjs b/dist/index.cjs
index a49032bc9823445ad1d7848ef75ddded2343c605..80233b8b7561db887496715c8c8fec5cb48c2177 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -884,6 +884,16 @@ var removeWhitespaces = (node) => {
   }
   return node;
 };
+var trustedTypesPolicy;
+var trustedHTML = (value) => {
+  if (!window.trustedTypes) {
+    return value;
+  }
+  trustedTypesPolicy ??= window.trustedTypes.createPolicy("tiptap-html", {
+    createHTML: (html) => html
+  });
+  return trustedTypesPolicy.createHTML(value);
+};
 function elementFromString(value) {
   if (typeof window === "undefined") {
     throw new Error(
@@ -891,7 +901,7 @@ function elementFromString(value) {
     );
   }
   const wrappedValue = `<body>${value}</body>`;
-  const html = new window.DOMParser().parseFromString(wrappedValue, "text/html").body;
+  const html = new window.DOMParser().parseFromString(trustedHTML(wrappedValue), "text/html").body;
   return removeWhitespaces(html);
 }
 
@@ -4384,7 +4394,7 @@ function createStyleTag(style2, nonce, suffix) {
     styleNode.setAttribute("nonce", nonce);
   }
   styleNode.setAttribute(`data-tiptap-style${suffix ? `-${suffix}` : ""}`, "");
-  styleNode.innerHTML = style2;
+  styleNode.textContent = style2;
   document.getElementsByTagName("head")[0].appendChild(styleNode);
   return styleNode;
 }
diff --git a/dist/index.js b/dist/index.js
index a6bddb1bb8f7561a6d98cab5e04635b69065a206..1ff2259080014e3bd28e54f4d5c4b3a9283f9481 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -717,6 +717,16 @@ var removeWhitespaces = (node) => {
   }
   return node;
 };
+var trustedTypesPolicy;
+var trustedHTML = (value) => {
+  if (!window.trustedTypes) {
+    return value;
+  }
+  trustedTypesPolicy ??= window.trustedTypes.createPolicy("tiptap-html", {
+    createHTML: (html) => html
+  });
+  return trustedTypesPolicy.createHTML(value);
+};
 function elementFromString(value) {
   if (typeof window === "undefined") {
     throw new Error(
@@ -724,7 +734,7 @@ function elementFromString(value) {
     );
   }
   const wrappedValue = `<body>${value}</body>`;
-  const html = new window.DOMParser().parseFromString(wrappedValue, "text/html").body;
+  const html = new window.DOMParser().parseFromString(trustedHTML(wrappedValue), "text/html").body;
   return removeWhitespaces(html);
 }
 
@@ -4222,7 +4232,7 @@ function createStyleTag(style2, nonce, suffix) {
     styleNode.setAttribute("nonce", nonce);
   }
   styleNode.setAttribute(`data-tiptap-style${suffix ? `-${suffix}` : ""}`, "");
-  styleNode.innerHTML = style2;
+  styleNode.textContent = style2;
   document.getElementsByTagName("head")[0].appendChild(styleNode);
   return styleNode;
 }
diff --git a/src/utilities/createStyleTag.ts b/src/utilities/createStyleTag.ts
index 70fbbc86375bf1e8d42fa503f762ca308cdf8d89..d2a630b6bf7c49ef429866c533feeccf3e20154e 100644
--- a/src/utilities/createStyleTag.ts
+++ b/src/utilities/createStyleTag.ts
@@ -14,7 +14,7 @@ export function createStyleTag(style: string, nonce?: string, suffix?: string):
   }
 
   styleNode.setAttribute(`data-tiptap-style${suffix ? `-${suffix}` : ''}`, '')
-  styleNode.innerHTML = style
+  styleNode.textContent = style
   document.getElementsByTagName('head')[0].appendChild(styleNode)
 
   return styleNode
diff --git a/src/utilities/elementFromString.ts b/src/utilities/elementFromString.ts
index a6af3486fc5735cb79909b0c8229fb48dfc50241..04785ffef93120e83cee882db74e195a6713bb92 100644
--- a/src/utilities/elementFromString.ts
+++ b/src/utilities/elementFromString.ts
@@ -14,6 +14,19 @@ const removeWhitespaces = (node: HTMLElement) => {
   return node
 }
 
+let trustedTypesPolicy: TrustedTypePolicy | undefined
+
+const trustedHTML = (value: string) => {
+  if (!window.trustedTypes) {
+    return value
+  }
+
+  trustedTypesPolicy ??= window.trustedTypes.createPolicy('tiptap-html', {
+    createHTML: html => html,
+  })
+  return trustedTypesPolicy.createHTML(value)
+}
+
 export function elementFromString(value: string): HTMLElement {
   if (typeof window === 'undefined') {
     throw new Error(
@@ -23,7 +36,7 @@ export function elementFromString(value: string): HTMLElement {
   // add a wrapper to preserve leading and trailing whitespace
   const wrappedValue = `<body>${value}</body>`
 
-  const html = new window.DOMParser().parseFromString(wrappedValue, 'text/html').body
+  const html = new window.DOMParser().parseFromString(trustedHTML(wrappedValue), 'text/html').body
 
   return removeWhitespaces(html)
 }
