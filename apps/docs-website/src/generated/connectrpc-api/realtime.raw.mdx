---
title: ConnectRPC API
description: Generated reference for Chatto's public ConnectRPC services.
editUrl: false
---

{/* Generated by protoc-gen-doc from proto/chatto/api/v1/*.proto. Do not edit directly. */}

Chatto exposes these ConnectRPC services below `/api/connect`.

Endpoint paths use the Connect convention:

`/api/connect/<fully-qualified-service>/<method>`

Use this reference when building clients against Chatto's public ConnectRPC API.


## Supporting Types

<a id="chatto-realtime-v1-RealtimeCaughtUp"></a>

### RealtimeCaughtUp

Confirms that durable replay is complete and live delivery has begun.

Clients can retain `cursor` after applying every preceding event. A cursor
must not outlive the projection state it describes. Events after this frame
are live. Transient events are never replayed. A cursor is usable for up to
24 hours after issue and should be replaced by each later caught-up cursor.

| Field | Type | Description |
| --- | --- | --- |
| `cursor` | `string` | Opaque cursor at the replay-to-live handoff boundary. |



<a id="chatto-realtime-v1-RealtimeClientFrame"></a>

### RealtimeClientFrame

Client-to-server frame for Chatto's protobuf WebSocket realtime protocol.

Clients send binary protobuf frames to `/api/realtime`. The first frame must
be `hello`; after the server replies with `hello`, clients send
`subscribe_events` to start the authenticated live event stream.

| Field | Type | Description |
| --- | --- | --- |
| `frame.hello` | [`RealtimeClientHello`](#chatto-realtime-v1-RealtimeClientHello) | Opens the protocol session and optionally carries a bearer token. |
| `frame.subscribe_events` | [`RealtimeSubscribeEvents`](#chatto-realtime-v1-RealtimeSubscribeEvents) | Starts the caller's authorized live-event stream. |
| `frame.ping` | [`RealtimePing`](#chatto-realtime-v1-RealtimePing) | Application-level ping. The server replies with `pong`. |



<a id="chatto-realtime-v1-RealtimeClientHello"></a>

### RealtimeClientHello

Initial client hello.

| Field | Type | Description |
| --- | --- | --- |
| `protocol_version` | `uint32` | Protocol version requested by the client. The only supported version is 2. |
| `bearer_token` | `optional string` | Optional bearer token. When present, it takes precedence over cookie auth. |



<a id="chatto-realtime-v1-RealtimeClose"></a>

### RealtimeClose

Close instruction sent before the socket is closed when possible.

| Field | Type | Description |
| --- | --- | --- |
| `code` | `string` | Stable machine-readable close code. |
| `message` | `string` | Human-readable diagnostic message. |
| `reconnect` | `bool` | True when the client should reconnect after a delay. |
| `retry_after_ms` | `uint32` | Suggested reconnect delay in milliseconds. |



<a id="chatto-realtime-v1-RealtimeError"></a>

### RealtimeError

Protocol error.

| Field | Type | Description |
| --- | --- | --- |
| `code` | `string` | Stable machine-readable error code. |
| `message` | `string` | Human-readable diagnostic message. |
| `fatal` | `bool` | True when the server will close the socket after sending this error. |



<a id="chatto-realtime-v1-RealtimeEventEnvelope"></a>

### RealtimeEventEnvelope

One authorized transient event delivered over the realtime WebSocket.

These events have no resume cursor and are never replayed. Durable state is
delivered only as RealtimeProjectionEvent operations.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable event ID. |
| `created_at` | `google.protobuf.Timestamp` | Time the event was created. |
| `actor_id` | `optional string` | User or system actor that caused the event, when known. |
| `event.user_typing` | [`RealtimeTypingEvent`](#chatto-realtime-v1-RealtimeTypingEvent) | A user is typing in a room or thread. |
| `event.presence_changed` | [`RealtimePresenceChangedEvent`](#chatto-realtime-v1-RealtimePresenceChangedEvent) | A user's live presence changed. |
| `event.notification_created` | [`RealtimeNotificationCreatedEvent`](#chatto-realtime-v1-RealtimeNotificationCreatedEvent) | A notification was created for the current user. |
| `event.notification_dismissed` | [`RealtimeNotificationDismissedEvent`](#chatto-realtime-v1-RealtimeNotificationDismissedEvent) | A notification was dismissed for the current user. |
| `event.notification_level_changed` | [`RealtimeNotificationLevelChangedEvent`](#chatto-realtime-v1-RealtimeNotificationLevelChangedEvent) | The current user's notification level changed. |
| `event.thread_follow_changed` | [`RealtimeThreadFollowChangedEvent`](#chatto-realtime-v1-RealtimeThreadFollowChangedEvent) | The current user's thread follow state changed. |
| `event.room_marked_as_read` | [`RealtimeRoomMarkedAsReadEvent`](#chatto-realtime-v1-RealtimeRoomMarkedAsReadEvent) | The current user's room read state changed. |
| `event.server_updated` | [`RealtimeServerUpdatedEvent`](#chatto-realtime-v1-RealtimeServerUpdatedEvent) | The server profile/config changed. |
| `event.user_profile_updated` | [`RealtimeUserProfileUpdatedEvent`](#chatto-realtime-v1-RealtimeUserProfileUpdatedEvent) | A user profile changed. |
| `event.server_user_preferences_updated` | [`RealtimeServerUserPreferencesUpdatedEvent`](#chatto-realtime-v1-RealtimeServerUserPreferencesUpdatedEvent) | The current user's display preferences changed. |
| `event.room_groups_updated` | [`RealtimeRoomGroupsUpdatedEvent`](#chatto-realtime-v1-RealtimeRoomGroupsUpdatedEvent) | Room-group layout changed. |
| `event.server_member_deleted` | [`RealtimeServerMemberDeletedEvent`](#chatto-realtime-v1-RealtimeServerMemberDeletedEvent) | A server member was deleted. |
| `event.mention_notification` | [`RealtimeMentionNotificationEvent`](#chatto-realtime-v1-RealtimeMentionNotificationEvent) | The current user was mentioned in a room. |
| `event.new_direct_message_notification` | [`RealtimeNewDirectMessageNotificationEvent`](#chatto-realtime-v1-RealtimeNewDirectMessageNotificationEvent) | The current user received a new direct message. |
| `event.session_terminated` | [`RealtimeSessionTerminatedEvent`](#chatto-realtime-v1-RealtimeSessionTerminatedEvent) | The current user's session was terminated. |



<a id="chatto-realtime-v1-RealtimeHeartbeat"></a>

### RealtimeHeartbeat

Application-level heartbeat.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable event ID for this heartbeat. |
| `created_at` | `google.protobuf.Timestamp` | Time the heartbeat was emitted. |



<a id="chatto-realtime-v1-RealtimeMentionNotificationEvent"></a>

### RealtimeMentionNotificationEvent

Mention attention signal for the connected user.

Inline names are display hints. Hydrate referenced rooms through
`RoomDirectoryService.BatchGetRooms` and users through
`UserService.BatchGetUsers` when local caches are missing or stale.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room where the mention occurred. |
| `actor_user_id` | `string` | Author user ID. |
| `room_name` | `optional string` | Display name of the room where the mention occurred, when hydrated for the caller. |
| `actor_display_name` | `optional string` | Display name of the author who mentioned the connected user, when hydrated. |



<a id="chatto-realtime-v1-RealtimeNewDirectMessageNotificationEvent"></a>

### RealtimeNewDirectMessageNotificationEvent

New-DM attention signal for the connected user.

Inline names and avatar URLs are display hints. Hydrate the DM room through
`RoomDirectoryService.GetRoom` or `RoomDirectoryService.BatchGetRooms`, and
the sender through `UserService.GetUser` or `UserService.BatchGetUsers` when
local caches are missing or stale.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | DM room ID. |
| `sender_id` | `string` | Sender user ID. |
| `sender_display_name` | `optional string` | Display name of the sender, when hydrated. |
| `sender_avatar_url` | `optional string` | Avatar URL of the sender, when one is available. |
| `conversation_name` | `optional string` | Display name for the DM conversation from the connected user's perspective, when hydrated. |



<a id="chatto-realtime-v1-RealtimeNotificationCreatedEvent"></a>

### RealtimeNotificationCreatedEvent

Notification-created signal for the connected user.

Hydrate full notification rows with `NotificationService.GetNotification` or
`NotificationService.BatchGetNotifications`. Use `RoomService` or
`ThreadService` anchor reads when `event_id` or `in_reply_to_id` must be
opened in context.

| Field | Type | Description |
| --- | --- | --- |
| `notification_id` | `string` | Notification ID. |
| `room_id` | `optional string` | Room associated with the notification, when the notification navigates to a room. |
| `event_id` | `optional string` | Event ID associated with the notification, when the notification navigates to an event. |
| `in_reply_to_id` | `optional string` | Parent event ID for reply notifications, when known. |
| `silent` | `bool` | True when the notification should not alert. |



<a id="chatto-realtime-v1-RealtimeNotificationDismissedEvent"></a>

### RealtimeNotificationDismissedEvent

Notification-dismissed signal for the connected user.

Remove the local notification row by ID. A later singular notification read
returns `NOT_FOUND`; batch reads omit dismissed or missing notifications.

| Field | Type | Description |
| --- | --- | --- |
| `notification_id` | `string` | Notification ID. |



<a id="chatto-realtime-v1-RealtimeNotificationLevelChangedEvent"></a>

### RealtimeNotificationLevelChangedEvent

Notification-level signal for the connected user.

The updated level is inline. Use
`NotificationPreferencesService.GetRoomNotificationPreference` when clients
need the complete preference resource.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room whose notification level changed. |
| `level` | `chatto.api.v1.NotificationLevel` | Explicit room notification level. |
| `effective_level` | `chatto.api.v1.NotificationLevel` | Effective notification level after defaults are applied. |



<a id="chatto-realtime-v1-RealtimePing"></a>

### RealtimePing

Application-level ping.

| Field | Type | Description |
| --- | --- | --- |
| `nonce` | `string` | Client-chosen opaque value echoed in the pong. |



<a id="chatto-realtime-v1-RealtimePong"></a>

### RealtimePong

Application-level pong.

| Field | Type | Description |
| --- | --- | --- |
| `nonce` | `string` | Echo of the ping nonce. |



<a id="chatto-realtime-v1-RealtimePresenceChangedEvent"></a>

### RealtimePresenceChangedEvent

Presence-changed signal.

The latest presence status is inline. Use `UserService.GetUser` when
the surrounding user profile or custom status also needs refreshing.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose presence changed. |
| `status` | `chatto.api.v1.PresenceStatus` | Latest presence status. |



<a id="chatto-realtime-v1-RealtimeProjectionActiveCallsReplace"></a>

### RealtimeProjectionActiveCallsReplace

Finite current active-call state visible to the caller.

| Field | Type | Description |
| --- | --- | --- |
| `calls` | `repeated chatto.api.v1.ActiveCall` | No field description provided. |



<a id="chatto-realtime-v1-RealtimeProjectionEvent"></a>

### RealtimeProjectionEvent

One ordered, idempotent mutation of the caller's authorised server view.

A fresh subscription starts with a reset operation followed by current
resource upserts. A resumed subscription derives operations from EVT after
the supplied cursor. Both paths use this envelope and the same client
reducer. The stream is a convergence feed, not an immutable audit log:
replay uses current authorisation, deletion, and erasure state.

| Field | Type | Description |
| --- | --- | --- |
| `id` | `string` | Stable event ID. Synthetic compacted-replay events use server-generated IDs. |
| `created_at` | `google.protobuf.Timestamp` | Time the source fact occurred or the compacted event was generated. |
| `actor_id` | `optional string` | User or system actor that caused the source fact, when known. |
| `resume_cursor` | `optional string` | Opaque cursor safe to retain with the resulting projection state after applying every operation in this event. |
| `operations` | repeated [`RealtimeProjectionOperation`](#chatto-realtime-v1-RealtimeProjectionOperation) | Operations applied atomically and in order by the client reducer. |



<a id="chatto-realtime-v1-RealtimeProjectionNotificationsReplace"></a>

### RealtimeProjectionNotificationsReplace

Finite current notification state emitted on bootstrap and every resume.

| Field | Type | Description |
| --- | --- | --- |
| `page` | `chatto.api.v1.ListNotificationsResponse` | Newest pending notifications and total pending count. |
| `room_counts` | `repeated chatto.api.v1.RoomNotificationCount` | Complete current counts for rooms with pending notifications. |



<a id="chatto-realtime-v1-RealtimeProjectionOperation"></a>

### RealtimeProjectionOperation

One mutation of a server-scoped client projection.

| Field | Type | Description |
| --- | --- | --- |
| `operation.reset` | [`RealtimeProjectionReset`](#chatto-realtime-v1-RealtimeProjectionReset) | Clears all canonical server projection state before compacted replay. |
| `operation.server_upsert` | `chatto.api.v1.ServerPublicProfile` | Replaces the public server profile. |
| `operation.viewer_upsert` | `chatto.api.v1.GetViewerResponse` | Replaces the authenticated viewer resource. |
| `operation.user_upsert` | `chatto.api.v1.DirectoryMember` | Adds or replaces one server directory member. |
| `operation.user_remove` | [`RealtimeProjectionUserRemove`](#chatto-realtime-v1-RealtimeProjectionUserRemove) | Removes one server directory member. |
| `operation.room_upsert` | [`RealtimeProjectionRoom`](#chatto-realtime-v1-RealtimeProjectionRoom) | Adds or replaces one room, its viewer state, and current membership. |
| `operation.room_remove` | [`RealtimeProjectionRoomRemove`](#chatto-realtime-v1-RealtimeProjectionRoomRemove) | Removes one room and all locally retained room state. |
| `operation.room_groups_replace` | [`RealtimeProjectionRoomGroupsReplace`](#chatto-realtime-v1-RealtimeProjectionRoomGroupsReplace) | Replaces the complete visible room-group layout. |
| `operation.room_timeline_replace` | [`RealtimeProjectionRoomTimelineReplace`](#chatto-realtime-v1-RealtimeProjectionRoomTimelineReplace) | Replaces the retained recent timeline window for one joined room. |
| `operation.room_timeline_event_upsert` | [`RealtimeProjectionRoomTimelineEventUpsert`](#chatto-realtime-v1-RealtimeProjectionRoomTimelineEventUpsert) | Adds or replaces one renderable event in a retained room timeline. |
| `operation.server_state_upsert` | [`RealtimeProjectionServerState`](#chatto-realtime-v1-RealtimeProjectionServerState) | Replaces authenticated server-wide presentation and runtime state. |
| `operation.room_timeline_event_remove` | [`RealtimeProjectionRoomTimelineEventRemove`](#chatto-realtime-v1-RealtimeProjectionRoomTimelineEventRemove) | Removes one event from a retained room timeline. This is distinct from a message tombstone: projection-only channel echoes disappear when the underlying thread reply is no longer echoed into the room. |
| `operation.notifications_replace` | [`RealtimeProjectionNotificationsReplace`](#chatto-realtime-v1-RealtimeProjectionNotificationsReplace) | Replaces the viewer's current pending-notification page and room counts. |
| `operation.room_viewer_state_replace` | [`RealtimeProjectionRoomViewerStateReplace`](#chatto-realtime-v1-RealtimeProjectionRoomViewerStateReplace) | Replaces one room's current viewer state without retransmitting room metadata or membership. |
| `operation.active_calls_replace` | [`RealtimeProjectionActiveCallsReplace`](#chatto-realtime-v1-RealtimeProjectionActiveCallsReplace) | Replaces every active call visible to the caller. |



<a id="chatto-realtime-v1-RealtimeProjectionReactionChange"></a>

### RealtimeProjectionReactionChange

Reaction transition retained for integrations while the containing timeline
row carries the authoritative aggregate reaction state.

| Field | Type | Description |
| --- | --- | --- |
| `action` | [`RealtimeProjectionReactionAction`](#chatto-realtime-v1-RealtimeProjectionReactionAction) | Whether the actor added or removed the reaction. |
| `message_event_id` | `string` | Canonical reacted-to message event ID. |
| `emoji` | `string` | Reaction emoji. |
| `user_id` | `string` | User who changed the reaction. |



<a id="chatto-realtime-v1-RealtimeProjectionReset"></a>

### RealtimeProjectionReset

Reset marker for a compacted projection replay.



<a id="chatto-realtime-v1-RealtimeProjectionRoom"></a>

### RealtimeProjectionRoom

Canonical room state retained by a server-scoped client projection.

| Field | Type | Description |
| --- | --- | --- |
| `room` | `chatto.api.v1.RoomWithViewerState` | Public room metadata and viewer-specific state. |
| `member_user_ids` | `repeated string` | Complete current room membership expressed as references into the projection's server-wide user directory. |
| `viewer_notification_count` | `uint32` | Current pending notifications targeting this room for the viewer. |



<a id="chatto-realtime-v1-RealtimeProjectionRoomGroupsReplace"></a>

### RealtimeProjectionRoomGroupsReplace

Complete ordered room-group layout visible to the caller.

| Field | Type | Description |
| --- | --- | --- |
| `groups` | `repeated chatto.api.v1.RoomGroup` | Visible room groups in sidebar order. |



<a id="chatto-realtime-v1-RealtimeProjectionRoomRemove"></a>

### RealtimeProjectionRoomRemove

Removes one room from the client projection.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Stable room ID to remove. |



<a id="chatto-realtime-v1-RealtimeProjectionRoomTimelineEventRemove"></a>

### RealtimeProjectionRoomTimelineEventRemove

Removes one projection-only row from a retained room timeline.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the retained row. |
| `event_id` | `string` | Stable event ID of the row to remove. |



<a id="chatto-realtime-v1-RealtimeProjectionRoomTimelineEventUpsert"></a>

### RealtimeProjectionRoomTimelineEventUpsert

Current renderable state for one timeline event.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the event. |
| `event` | `chatto.api.v1.RoomTimelineEvent` | Current renderable event state. |
| `includes` | `chatto.api.v1.RoomTimelineIncludes` | Related users needed to render this event. |
| `reaction_change` | optional [`RealtimeProjectionReactionChange`](#chatto-realtime-v1-RealtimeProjectionReactionChange) | Reaction transition that caused this upsert, when applicable. |
| `retain_deleted_row` | `bool` | Preserve a deleted echo row as a tombstone. Directly deleted echoes use a remove operation instead; this flag identifies canonical-reply deletion. |
| `event_cursor` | `string` | Opaque room-timeline cursor for this canonical row. Clients use retained row cursors to advance a bounded window without a separate refresh read. |



<a id="chatto-realtime-v1-RealtimeProjectionRoomTimelineReplace"></a>

### RealtimeProjectionRoomTimelineReplace

Complete retained recent timeline window for one room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room whose retained window is replaced. |
| `page` | `chatto.api.v1.RoomTimelinePage` | Renderable current timeline state. Deleted and crypto-shredded message bodies are absent and represented by their normal tombstone fields. |
| `event_cursors` | map&lt;`string`, `string`&gt; | Opaque cursor for every retained row, keyed by timeline event ID. |



<a id="chatto-realtime-v1-RealtimeProjectionRoomViewerStateReplace"></a>

### RealtimeProjectionRoomViewerStateReplace

Lightweight current viewer state for one projected room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | No field description provided. |
| `viewer_state` | `chatto.api.v1.RoomViewerState` | No field description provided. |



<a id="chatto-realtime-v1-RealtimeProjectionServerState"></a>

### RealtimeProjectionServerState

Authenticated server state required by every signed-in client.

| Field | Type | Description |
| --- | --- | --- |
| `motd` | `optional string` | Optional message of the day shown to authenticated members. |
| `runtime` | `chatto.api.v1.ServerRuntimeConfig` | Runtime capabilities and limits advertised to the client. |



<a id="chatto-realtime-v1-RealtimeProjectionUserRemove"></a>

### RealtimeProjectionUserRemove

Removes one user from the client projection.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Stable user ID to remove. |



<a id="chatto-realtime-v1-RealtimeRoomGroupsUpdatedEvent"></a>

### RealtimeRoomGroupsUpdatedEvent

Room-group layout signal.

Refetch room-group layout with `RoomDirectoryService.ListRoomGroups`. If a
client already knows specific group IDs, it can use
`RoomDirectoryService.GetRoomGroup` or
`RoomDirectoryService.BatchGetRoomGroups`.

| Field | Type | Description |
| --- | --- | --- |
| `changed` | `bool` | Always true; present for event-bus compatibility. |



<a id="chatto-realtime-v1-RealtimeRoomMarkedAsReadEvent"></a>

### RealtimeRoomMarkedAsReadEvent

Room-read signal for the connected user.

This is a current-user read-state invalidation signal. Clients can patch local
unread state for the room; no separate hydration is normally needed.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room marked read. |



<a id="chatto-realtime-v1-RealtimeServerFrame"></a>

### RealtimeServerFrame

Server-to-client frame for Chatto's protobuf WebSocket realtime protocol.

| Field | Type | Description |
| --- | --- | --- |
| `frame.hello` | [`RealtimeServerHello`](#chatto-realtime-v1-RealtimeServerHello) | Confirms protocol version, server version, and capabilities. |
| `frame.subscribed` | [`RealtimeSubscribed`](#chatto-realtime-v1-RealtimeSubscribed) | Confirms the live-event stream has started. |
| `frame.event` | [`RealtimeEventEnvelope`](#chatto-realtime-v1-RealtimeEventEnvelope) | A live event authorized for the connected user. |
| `frame.heartbeat` | [`RealtimeHeartbeat`](#chatto-realtime-v1-RealtimeHeartbeat) | Application-level heartbeat used for liveness checks. |
| `frame.error` | [`RealtimeError`](#chatto-realtime-v1-RealtimeError) | Protocol or authorization error. |
| `frame.close` | [`RealtimeClose`](#chatto-realtime-v1-RealtimeClose) | Server-requested close with reconnect guidance. |
| `frame.pong` | [`RealtimePong`](#chatto-realtime-v1-RealtimePong) | Reply to a client ping. |
| `frame.caught_up` | [`RealtimeCaughtUp`](#chatto-realtime-v1-RealtimeCaughtUp) | Confirms that durable replay reached the live-stream boundary. |
| `frame.projection_event` | [`RealtimeProjectionEvent`](#chatto-realtime-v1-RealtimeProjectionEvent) | One idempotent mutation of the caller's server projection. |



<a id="chatto-realtime-v1-RealtimeServerHello"></a>

### RealtimeServerHello

Initial server hello.

| Field | Type | Description |
| --- | --- | --- |
| `protocol_version` | `uint32` | Protocol version accepted by the server. |
| `server_version` | `string` | Chatto server software version. |
| `heartbeat_interval_seconds` | `uint32` | Approximate heartbeat interval clients should expect. |
| `capabilities` | `repeated string` | Stable protocol capability keys supported by this server. Current keys: `chatto.realtime.events.live.v1`, `chatto.realtime.heartbeat.v1`, and `chatto.realtime.ping.v1`, `chatto.realtime.events.resume.v1`, and `chatto.realtime.projection.v1`. |



<a id="chatto-realtime-v1-RealtimeServerMemberDeletedEvent"></a>

### RealtimeServerMemberDeletedEvent

Server-member deleted signal.

Remove or invalidate local member/user rows for `user_id`. Admin clients can
hydrate remaining member rows through `AdminUserService.GetMember` or
`AdminUserService.BatchGetMembers`.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Deleted user ID. |



<a id="chatto-realtime-v1-RealtimeServerUpdatedEvent"></a>

### RealtimeServerUpdatedEvent

Transient server-profile signal.

Public server profile fields are inline. Use `ServerDiscoveryService.GetServer`
for public metadata and `ServerService.GetMotd` for the authenticated MOTD.
Canonical public and authenticated server state also converges through
projection operations.

| Field | Type | Description |
| --- | --- | --- |
| `name` | `string` | Server display name. |
| `description` | `string` | Server description. |
| `logo_url` | `optional string` | Server logo URL, when configured. |
| `banner_url` | `optional string` | Server banner URL, when configured. |



<a id="chatto-realtime-v1-RealtimeServerUserPreferencesUpdatedEvent"></a>

### RealtimeServerUserPreferencesUpdatedEvent

User display-preferences signal for the connected user.

The updated display preferences are inline for the connected user.

| Field | Type | Description |
| --- | --- | --- |
| `timezone` | `optional string` | IANA timezone name, when the user has selected a fixed timezone. |
| `time_format` | `chatto.api.v1.TimeFormat` | Preferred time display format. |



<a id="chatto-realtime-v1-RealtimeSessionTerminatedEvent"></a>

### RealtimeSessionTerminatedEvent

Session-terminated signal for the connected user's current session.

| Field | Type | Description |
| --- | --- | --- |
| `reason` | `string` | Termination reason. |



<a id="chatto-realtime-v1-RealtimeSubscribeEvents"></a>

### RealtimeSubscribeEvents

Request to start the caller's authorized live event stream.

| Field | Type | Description |
| --- | --- | --- |
| `resume_cursor` | `optional string` | Opaque cursor from a previously received durable event or caught-up frame. The server replays authorized durable projection changes after this cursor before continuing with live delivery. Omit it to receive a compacted reset of the current authorised server projection. Cursors expire 24 hours after issue; an expired cursor also receives a compacted reset rather than historical replay. |



<a id="chatto-realtime-v1-RealtimeSubscribed"></a>

### RealtimeSubscribed

Confirms event streaming has started.

| Field | Type | Description |
| --- | --- | --- |
| `start_cursor` | `optional string` | Opaque cursor from which this subscription starts. When the request supplied a resume cursor, this is that validated cursor. Otherwise it is the current live boundary. |



<a id="chatto-realtime-v1-RealtimeThreadFollowChangedEvent"></a>

### RealtimeThreadFollowChangedEvent

Thread-follow signal for the connected user.

The updated follow state is inline. Use `ThreadService.GetThreadEvents`
or `ThreadService.GetThreadEventsAround` when the thread itself needs
refreshing.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room containing the thread. |
| `thread_root_event_id` | `string` | Root message event ID of the thread. |
| `following` | `bool` | True when the user now follows the thread. |



<a id="chatto-realtime-v1-RealtimeTypingEvent"></a>

### RealtimeTypingEvent

Typing signal.

This is an ephemeral signal. `room_id` and `thread_root_event_id` identify
where to display typing state; clients normally do not hydrate it.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Room where the actor is typing. |
| `thread_root_event_id` | `optional string` | Thread root event ID when the actor is typing in a thread. |



<a id="chatto-realtime-v1-RealtimeUserProfileUpdatedEvent"></a>

### RealtimeUserProfileUpdatedEvent

User-profile signal.

Basic profile fields are inline. Use `UserService.GetUser` or
`UserService.BatchGetUsers` for complete user-profile hydration.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | User whose profile changed. |
| `login` | `string` | Login name. |
| `display_name` | `string` | Display name. |
| `avatar_url` | `optional string` | Avatar URL, when the user has an avatar. |




## Enums

<a id="chatto-realtime-v1-RealtimeProjectionReactionAction"></a>

### RealtimeProjectionReactionAction

Kind of reaction transition.

| Name | Number | Description |
| --- | --- | --- |
| `REALTIME_PROJECTION_REACTION_ACTION_UNSPECIFIED` | `0` | Unknown action. |
| `REALTIME_PROJECTION_REACTION_ACTION_ADDED` | `1` | A reaction was added. |
| `REALTIME_PROJECTION_REACTION_ACTION_REMOVED` | `2` | A reaction was removed. |
