syntax = "proto3";
package chatto.core.webhook.v1;
import "google/protobuf/timestamp.proto";
option go_package = "hmans.de/chatto/internal/pb/chatto/core/webhook/v1;webhookv1";

// One pending job in the JOBS work queue. JetStream owns retry state.
// References retain no message plaintext or destination credentials.
message BotWebhookDelivery {
  string delivery_id = 1;
  string bot_user_id = 2;
  string webhook_id = 3;
  string source_event_id = 4;
  string room_id = 5;
  repeated string triggers = 6;
  google.protobuf.Timestamp occurred_at = 7;
  // Immutable source-time deadline; restart and restore never extend it.
  google.protobuf.Timestamp expires_at = 8;
  uint32 max_attempts = 9;
  int64 retry_delay_ms = 10;
}
