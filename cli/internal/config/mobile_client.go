package config

const (
	// ChattoMobileClientID identifies the official native mobile OAuth client.
	ChattoMobileClientID = "eu.chattocorp.chatto.mobile"
	// ChattoMobileOAuthCallback is the exact system authentication session callback.
	ChattoMobileOAuthCallback = "eu.chattocorp.chatto.mobile:/oauth/callback"
)
