---
title: MemberDirectoryService
description: Server and room member directory RPCs.
editUrl: false
---

{/* Generated from proto/chatto/{api,admin,realtime}/v1/*.proto. Do not edit directly. */}

Chatto exposes this service below `/api/connect`.



Shared message and enum definitions are documented in [Shared Types And Enums](/reference/connectrpc-api/types/).



<a id="chatto-api-v1-MemberDirectoryService"></a>

## MemberDirectoryService

Reads server and room member directories.

<a id="chatto-api-v1-MemberDirectoryService-ListServerMembers"></a>

### ListServerMembers

Lists authenticated server members. Every authenticated user is a server
member; admin-sensitive fields stay out of this public row shape.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/ListServerMembers
```

<a id="chatto-api-v1-ListServerMembersRequest"></a>

#### Input: ListServerMembersRequest

Request for server members visible to the authenticated user.

| Field | Type | Description |
| --- | --- | --- |
| `search` | `string` | Optional case-insensitive search against login and display name. |
| `page` | [`PageRequest`](/reference/connectrpc-api/types/#chatto-api-v1-PageRequest) | Page request. Defaults to 20 results when absent or limit is zero. |


<a id="chatto-api-v1-ListServerMembersResponse"></a>

#### Result: ListServerMembersResponse

Server member page.

| Field | Type | Description |
| --- | --- | --- |
| `members` | repeated [`DirectoryMember`](/reference/connectrpc-api/types/#chatto-api-v1-DirectoryMember) | Members in the requested page. |
| `page` | [`PageInfo`](/reference/connectrpc-api/types/#chatto-api-v1-PageInfo) | Page metadata. |


<a id="chatto-api-v1-MemberDirectoryService-GetServerMember"></a>

### GetServerMember

Gets one authenticated server member. Returns NOT_FOUND when the user ID is
unknown.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/GetServerMember
```

<a id="chatto-api-v1-GetServerMemberRequest"></a>

#### Input: GetServerMemberRequest

Request one authenticated server member by stable user ID.

| Field | Type | Description |
| --- | --- | --- |
| `user_id` | `string` | Required target user ID. |


<a id="chatto-api-v1-GetServerMemberResponse"></a>

#### Result: GetServerMemberResponse

Server member response.

| Field | Type | Description |
| --- | --- | --- |
| `member` | [`DirectoryMember`](/reference/connectrpc-api/types/#chatto-api-v1-DirectoryMember) | Requested member. |


<a id="chatto-api-v1-MemberDirectoryService-BatchGetServerMembers"></a>

### BatchGetServerMembers

Gets authenticated server member rows for multiple users.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/BatchGetServerMembers
```

<a id="chatto-api-v1-BatchGetServerMembersRequest"></a>

#### Input: BatchGetServerMembersRequest

Request server member rows for a set of stable user IDs.

| Field | Type | Description |
| --- | --- | --- |
| `user_ids` | `repeated string` | Required target user IDs. Unknown IDs are omitted from the response. |


<a id="chatto-api-v1-BatchGetServerMembersResponse"></a>

#### Result: BatchGetServerMembersResponse

Batch server member response.

| Field | Type | Description |
| --- | --- | --- |
| `members` | repeated [`DirectoryMember`](/reference/connectrpc-api/types/#chatto-api-v1-DirectoryMember) | Found members. The server preserves first-seen request order and de-duplicates repeated IDs. |


<a id="chatto-api-v1-MemberDirectoryService-ListRoomMembers"></a>

### ListRoomMembers

Lists explicit members of a room. The caller must be a member of the room.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/ListRoomMembers
```

<a id="chatto-api-v1-ListRoomMembersRequest"></a>

#### Input: ListRoomMembersRequest

Request for members of one room.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required. Room whose explicit members should be listed. |
| `search` | `string` | Optional case-insensitive search against login and display name. |
| `page` | [`PageRequest`](/reference/connectrpc-api/types/#chatto-api-v1-PageRequest) | Page request. Defaults to 20 results when absent or limit is zero. |


<a id="chatto-api-v1-ListRoomMembersResponse"></a>

#### Result: ListRoomMembersResponse

Room member page.

| Field | Type | Description |
| --- | --- | --- |
| `members` | repeated [`DirectoryMember`](/reference/connectrpc-api/types/#chatto-api-v1-DirectoryMember) | Members in the requested page. |
| `page` | [`PageInfo`](/reference/connectrpc-api/types/#chatto-api-v1-PageInfo) | Page metadata. |


<a id="chatto-api-v1-MemberDirectoryService-GetRoomMember"></a>

### GetRoomMember

Gets one explicit member of a room. The caller must be a member of the
room. Returns NOT_FOUND when the target is unknown or not a room member.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/GetRoomMember
```

<a id="chatto-api-v1-GetRoomMemberRequest"></a>

#### Input: GetRoomMemberRequest

Request one member of one room by stable user ID.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required room ID. |
| `user_id` | `string` | Required target user ID. |


<a id="chatto-api-v1-GetRoomMemberResponse"></a>

#### Result: GetRoomMemberResponse

Room member response.

| Field | Type | Description |
| --- | --- | --- |
| `member` | [`DirectoryMember`](/reference/connectrpc-api/types/#chatto-api-v1-DirectoryMember) | Requested room member. |


<a id="chatto-api-v1-MemberDirectoryService-BatchGetRoomMembers"></a>

### BatchGetRoomMembers

Gets explicit room member rows for multiple users. The caller must be a
member of the room.

```http
POST /api/connect/chatto.api.v1.MemberDirectoryService/BatchGetRoomMembers
```

<a id="chatto-api-v1-BatchGetRoomMembersRequest"></a>

#### Input: BatchGetRoomMembersRequest

Request room member rows for a set of stable user IDs.

| Field | Type | Description |
| --- | --- | --- |
| `room_id` | `string` | Required room ID. |
| `user_ids` | `repeated string` | Required target user IDs. Unknown IDs and users that are not members of the room are omitted from the response. |


<a id="chatto-api-v1-BatchGetRoomMembersResponse"></a>

#### Result: BatchGetRoomMembersResponse

Batch room member response.

| Field | Type | Description |
| --- | --- | --- |
| `members` | repeated [`DirectoryMember`](/reference/connectrpc-api/types/#chatto-api-v1-DirectoryMember) | Found members. The server preserves first-seen request order and de-duplicates repeated IDs. |
